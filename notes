 cam

 utiliser systematiquement strncmp_m au lieu de strcmp ? (doublon + securite du max 'n')


 Penser a implementer le code d erreur si messages d'erreurs de fonctions
 = updater le main->last_status avec le code d erreur en question.



mettre is_builtin.c dans les helpers ?


À l'école 42, les correcteurs testent systématiquement le "Empty Env" (env -i). 
Ton shell doit être capable de survivre et de fonctionner 
même si l'environnement est vide au démarrage.